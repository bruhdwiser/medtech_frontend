4-Hour MedTech MVP Sprint PlanPhase 1: Environment & Scaffolding (45 Mins)Goal: Initialize the project and define the data structure.Claude Code Command: Init a Next.js project with Tailwind CSS and Lucide React. Create a /data folder with a mock JSON for patients including: id, name, age, location, disease_history, and prescriptions. Step-by-Step Execution:Setup: Initialize the Next.js app.Types: Define TypeScript interfaces for Patient and MedicalRecord.Mock Data: Create a patients.json file with 5-10 sample entries to make the dashboard look "alive" immediately.Phase 2: Core Patient Management (1 Hour)Goal: Create the registration form and the QR generation logic.Claude Code Command: Build a /register page with a form to add new patients. On submission, generate a unique QR code using the 'qrcode.react' library that links to /patient/[id]. Step-by-Step Execution:Registration Form: Build a clean UI for entering name, age, location, and initial diagnosis.QR Generation: Install qrcode.react. Display the QR code on a "Smart Health Card" component after registration.Persistence: Write a simple API route to POST new data into your local JSON file.Phase 3: Clinical Interface (1 Hour)Goal: Scan-to-view and record updating for doctors.Claude Code Command: Create a dynamic route /patient/[id] that displays medical history. Add an 'Update Record' form to append new prescriptions or diagnoses to the patient's history. Step-by-Step Execution:Patient View: Design a clean, read-only view of the patient's history.Update Module: Add a simple text area for doctors to add a "New Visit" entry with a timestamp.Navigation: Ensure the QR code actually points to this URL so a "scan" simulation works.Phase 4: Disease Surveillance Dashboard (45 Mins)Goal: Analytics and trend visualization for health authorities.Claude Code Command: Create an /admin dashboard using 'recharts'. Visualize the number of cases by disease type (Bar Chart) and cases by location (Pie Chart or List). Step-by-Step Execution:Data Aggregation: Write a helper function to count occurrences of diseases and locations from your JSON.Visuals: Implement a Bar Chart showing the top 5 most common diseases.Location Heatmap (Optional Lite Version): A simple list of "Hotspots" (locations with >3 cases) to satisfy the "trend identification" requirement.Phase 5: Final Polish & Demo Prep (30 Mins)Goal: UI cleanup and "The Golden Path" walkthrough.Claude Code Command: Review the UI for mobile responsiveness. Add a navigation sidebar to toggle between 'Register', 'Doctor View', and 'Admin Dashboard'.Final Checklist:[ ] Does the QR code work?[ ] Can I add a new diagnosis?[ ] Does the Dashboard update when a new patient is added?